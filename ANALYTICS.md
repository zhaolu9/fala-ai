# Fala aí! 数据后台框架

写给部署之后照着建的一份设计。现在不用做，但埋点的位置和字段先定死，免得以后返工。

---

## 一、先立三条原则

1. **匿名优先**：不存姓名、邮箱、手机号。每台设备生成一个随机 ID（如 `u_8f3k2m`），存在本地，仅用于区分"同一个人"和"不同人"。
2. **句子内容要不要收，分开决定**：使用行为（点了什么、成功没成功）默认收；用户输入的中文句子属于内容，敏感度高，单独对待（见第四节）。
3. **先问再收**：产品里已加首次使用的同意弹窗，输入框旁有常驻提示。任何新增采集，先更新这两处文案。

---

## 二、核心指标（分三层看）

### 第 1 层：活没活（回答"有人用吗"）

| 指标 | 定义 | 为什么看 |
|---|---|---|
| 新增用户 | 每天首次打开的设备数 | 传播有没有起效 |
| 次日/7 日回访 | 首次使用后第 2 天、第 7 天还回来的比例 | **最重要的单一指标**。语言学习靠重复，不回来 = 产品没用 |
| 人均使用天数 | 30 天内活跃天数 | 区分"看一眼"和"真在用" |
| 语言分布 | 各语言被选择的次数 | 决定下一个该做哪门语言 |

### 第 2 层：卡在哪（回答"我该修什么"）

| 指标 | 定义 | 为什么看 |
|---|---|---|
| **翻译成功率** | 成功句数 ÷ 尝试句数 | 直接反映 AI 网关是否稳。低于 90% 必须先修这个 |
| 翻译耗时 | 从点击到出结果的中位数 | 超过 8 秒用户就会放弃 |
| 手动添加占比 | 用手动录入的句子 ÷ 总新增句子 | 这个比例高 = 自动翻译不好用 |
| **加了句子后是否去练** | 加句用户中，24h 内进过跟读/在练的比例 | **产品闭环的核心指标**。加了不练 = 只是个翻译器，没成为学习工具 |
| 流失步骤 | 会话结束前最后停留的 tab | 找出劝退点 |
| 语言生成失败率 | "＋"新增语言的失败比例 | 决定要不要把更多语言改成预置 |

### 第 3 层：学得怎么样（回答"这产品有没有真价值"）

| 指标 | 定义 | 为什么看 |
|---|---|---|
| 在练句子数分布 | 每人平均放几句在练 | 验证"一次一两句"的假设是否成立 |
| **出师率** | 出师句数 ÷ 加入在练句数 | 有人真的练成了吗——最能证明价值的指标 |
| 出师周期 | 从加入在练到出师的天数中位数 | 一句话大概需要练几天 |
| 跟读使用率 | 用过跟读的用户比例 | 跟读是差异化功能，没人用就该重做 |
| 搬家使用率 | 用过跨语言搬家的比例 | 验证"多语言复用"这个假设 |

---

## 三、埋点清单（按发生顺序）

每条事件都带：`user_id`（匿名）、`ts`（时间戳）、`lang`（当前语言）。

| 事件名 | 触发时机 | 额外字段 |
|---|---|---|
| `app_open` | 打开应用 | `is_first`（是否首次） |
| `consent_accept` | 点击同意 | — |
| `lang_switch` | 切换语言 | `from`, `to` |
| `lang_create` | 用"＋"新增语言 | `input_raw`（用户输入的语言名）, `success`, `error_msg` |
| `phrase_add_attempt` | 点击生成卡片 | `count`（本次几句） |
| `phrase_add_result` | 每句翻译完成 | `success`, `retries`, `duration_ms`, `error_msg`, `scene`（AI 归的类） |
| `phrase_manual_add` | 手动录入保存 | — |
| `phrase_edit` | 编辑句子保存 | `is_builtin`（改的是内置句还是自己的） |
| `phrase_delete` | 删除句子 | `is_builtin` |
| `practice_add` | 加入在练 | `source`（从添加句子进 / 从常用句进） |
| `practice_graduate` | 点击出师 | `days_in_practice`（练了几天） |
| `play_audio` | 播放发音 | `where`（发音页/常用句/在练/跟读）, `rate`（正常/慢速） |
| `tutor_attempt` | 跟读一次 | `input_method`（麦克风/手动输入）, `score`, `success` |
| `scene_filter` | 点击场景按钮 | `scene` |
| `move_to_lang` | 跨语言搬家 | `mode`（在练/全部）, `target_lang`, `count`, `success_count` |
| `pronunciation_expand` | 点开拼读表格子 | `syllable` |

---

## 四、用户输入的中文句子：单独处理

这是最有价值、也最敏感的数据。**这些输入告诉你人们在异国到底想说什么**——如果一百个人都在加"我对XX过敏"，那它就该做成内置内容；某个场景输入量特别集中，那就是下一个该深耕的方向。

处理原则：

1. **默认只存归类，不存原文**。即 `phrase_add_result` 只记 `scene`（交通出行/买东西/…），不记具体句子。
2. **要存原文，必须做三件事**：在同意弹窗里明确写出来；做敏感信息过滤（数字串、门牌号、疑似证件号自动打码）；只存中文原句，不关联 `user_id`（切断人和内容的联系）。
3. **给用户一个退出开关**：设置里放"不要用我的句子改进产品"。

这一层建议**上线后第二阶段再做**，第一阶段先只看 `scene` 分布，已经足够指导方向。

---

## 五、技术实现（部署时）

- **数据库**：Supabase 免费版够用。两张表：`events`（埋点流水）、`daily_stats`（每日汇总，避免每次查询都扫全表）。
- **上报方式**：前端攒一批（比如 10 条或 5 秒）一起发到 `/api/track`，减少请求数。失败就丢弃，绝不能因为埋点失败影响用户使用。
- **网关复用**：`api/generate.js` 已经是所有 AI 请求的必经关卡，翻译的成功率、耗时、错误直接在那里记，最准确。
- **看板**：初期不用做界面，Supabase 自带的 SQL 查询 + 几条常用语句就够。有需要再上 Metabase（免费开源）。

---

## 六、上线后按这个顺序看

**第 1 周**：只看两个数——有多少人打开、翻译成功率。这两个决定产品能不能用。

**第 1 个月**：看次日回访和"加了句子后是否去练"。前者说明有没有价值，后者说明闭环通不通。

**第 3 个月**：看出师率和出师周期。这时候才谈得上"这个方法是不是真的有效"，也才有资格谈收费。
