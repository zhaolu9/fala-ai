# Fala aí! 部署指南（Cloudflare Pages）

代码已经在 GitHub 上了，剩下的都是点按钮，大约 10 分钟。

---

## 第 1 步 · 连接 GitHub 仓库

1. 打开 **dash.cloudflare.com** 并登录

2. 左侧菜单点 **`Workers 和 Pages`**（Workers & Pages）

3. 点 **`创建`**（Create）→ 选 **`Pages`** 标签页 → 点 **`连接到 Git`**（Connect to Git）

4. 授权 Cloudflare 访问你的 GitHub，然后在列表里选中 **`fala-ai`** 仓库 → **`开始设置`**（Begin setup）

---

## 第 2 步 · 填写构建配置

这一步最关键，三个地方要填对：

| 项目 | 填什么 |
|---|---|
| **项目名称** | `fala-ai`（这会决定你的网址） |
| **框架预设** Framework preset | 选 **`Vite`** |
| **构建命令** Build command | `npm run build` |
| **构建输出目录** Build output directory | `dist` |

> 选了 Vite 之后，后两项通常会自动填好。核对一遍就行。

---

## 第 3 步 · 添加环境变量

在同一个页面往下找 **`环境变量`**（Environment variables）：

- **变量名**：`DEEPSEEK_API_KEY`
- **值**：粘贴你在 platform.deepseek.com 创建的那串 key

> ⚠️ 变量名必须**一字不差**，大小写也要对。

可选：再加一条 `DAILY_TOTAL_CAP` = `500`，给自己上一道每日总调用量的保险。

**如果还没解决付款、暂时没有 key**：这一步可以先跳过，直接部署。网站能正常打开，发音、常用句、在练、跟读都能用，只有「添加句子」的 AI 翻译会提示未配置。等有 key 了，回到项目的 `设置 → 环境变量` 补上，然后重新部署一次即可。

---

## 第 4 步 · 部署

点 **`保存并部署`**（Save and Deploy），等一两分钟。

完成后会给你一个网址，形如：
`https://fala-ai.pages.dev`

✅ **完成标志**：手机打开这个网址，页面正常显示，点句子能听到发音。

---

## 完成之后

- **这个网址就是你的**，可以发给任何人。
- **以后改代码**：推到 GitHub，Cloudflare 会自动重新部署，不用再操作。
- **想要好记的域名**：项目 → `自定义域`（Custom domains），绑定自己买的域名。

---

## 遇到问题怎么办

| 现象 | 原因 / 解决 |
|---|---|
| 构建失败 | 多半是「构建输出目录」没填 `dist`。去 `设置 → 构建和部署` 改，然后重新部署 |
| 页面打开是白屏 | 等一分钟刷新；仍不行把 Cloudflare 的构建日志发给 Claude |
| 翻译提示"服务器未配置 API key" | 环境变量名必须是 `DEEPSEEK_API_KEY`，加完必须重新部署才生效 |
| 翻译一直失败 | 去 platform.deepseek.com 看余额是否为 0 |
| 有声音但读得不对 | 手机缺少该语言的语音包，去系统设置里下载「葡萄牙语（巴西）」 |

---

## 上线后立刻做的两件事

1. **自己完整用一遍**：加一句、加入在练、排序拖动、跟读、跨语言搬家。确认都正常再发出去。

2. **找个在国内的朋友打开试试**：这一步能立刻告诉你国内访问通不通，比任何推测都准。结果会决定你要不要走域名备案那条路。

---

## 想开数据统计时

把 `src/App.jsx` 里的 `const SEND = false;` 改成 `true`，推到 GitHub 即可。
埋点已经全部就位，详见 `ANALYTICS.md`。


---

## 换 AI 供应商

网关文件 `functions/api/generate.js` 顶部有一个 `PROVIDER` 配置块，
换供应商只改那三行（地址、模型名、环境变量名），前端一个字都不用动。

注释里已经写好了换回 Claude 的配置，复制粘贴即可。
支持 OpenAI 兼容格式的服务商（DeepSeek、智谱、通义等）都能这样接。
